pipeline {
    agent any

    stages {
        stage('Unit Tests - Backend') {
            steps {
                dir('bugtracker-backend') {
                    echo 'Running unit tests for backend...'
                    go test -v ./...
                }
            }
        }
    }
}
